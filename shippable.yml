language: scala

scala:
  - 2.13.0

build:
  ci:
    - sbt ";clean ;coverage ;test ;coverageReport ;coverageAggregate"
    - cp target/scala-2.13/coverage-report/cobertura.xml shippable/codecoverage
    - ls -lRt shippable

jdk:
  - openjdk11