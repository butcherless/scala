language: scala

jdk:
  - oraclejdk11

scala:
  - 2.13.2

build:
  ci:
    - sbt clean depAnalyzer/test json4sUtils/test dependencyUpdates
    # fix until jdk11 image available,   - sbt clean test dependencyUpdates
    # - cp target/scala-2.13/coverage-report/cobertura.xml shippable/codecoverage
    - ls -lRt shippable
