language: scala

scala:
  - 2.12.6

build:
  ci:
    - sbt ";clean ;assembly"
    - mkdir -p shippable/codecoverage
    - cp akka-http-hw/target/scala-2.12/scoverage-report/scoverage.xml shippable/codecoverage
    - spring-scala/gradlew clean build -b spring-scala/build.gradle

jdk:
  - oraclejdk8