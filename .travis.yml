language: scala
group: travis_lts
scala:
  - 2.12.8

addons:
  apt:
    update: true

script:
  - sbt ";clean ;coverage ;assembly ;coverageReport"
  - sbt coverageAggregate
after_success:
  - sbt coveralls

cache:
  directories:
    - $HOME/.gradle
